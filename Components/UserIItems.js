import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UserIItems = () => {
  return (
    <View>
      <Text>UserIItems</Text>
    </View>
  );
};

export default UserIItems;

const styles = StyleSheet.create({});
